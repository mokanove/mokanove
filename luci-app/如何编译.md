# 如何编译luci-app的通用教程
## 首先 需要一个Linux系统 和make环境
> 警告：不要使用root用户编译
>
> 本教程仅适用X86的SDK 如果希望编译其他架构 请自行下载对应SDK
>
> 以ArchLinux系统为例
```
sudo pacman -Syu --needed base-devel git gawk ncurses openssl zlib \
    wget curl unzip python3 python-setuptools fillup rsync \
    libxslt libxml2 boost findutils
```
## 准备SDK
```
curl -LO https://downloads.immortalwrt.org/releases/24.10.5/targets/x86/64/immortalwrt-sdk-24.10.5-x86-64_gcc-13.3.0_musl.Linux-x86_64.tar.zst
如果你在中国 可以试试这个:
curl -LO https://mirrors.cernet.edu.cn/immortalwrt/releases/24.10.5/targets/x86/64/immortalwrt-sdk-24.10.5-x86-64_gcc-13.3.0_musl.Linux-x86_64.tar.zst
```
## 解压压缩包
```
tar -xvf immortalwrt-sdk-24.10.5-x86-64_gcc-13.3.0_musl.Linux-x86_64.tar.zst
```
## 初始化SDK
> 请确保你的Git可以连接GitHub并正常克隆
```
cd immortalwrt-sdk-24.10.5-x86-64_gcc-13.3.0_musl.Linux-x86_64
make defconfig
./scripts/feeds update -a && ./scripts/feeds install -a
```
## 定位到包目录并克隆项目源码
```
cd immortalwrt-sdk-24.10.5-x86-64_gcc-13.3.0_musl.Linux-x86_64/package/
git clone [URL]
```
## 醒来吧亲爱的
> 请先准备好项目源代码和依赖并定位到SDK根目录
```
make package/project-directory-name/compile V=s -j$(nproc)
```
## 齐活
```
cd bin/packages/x86_64/base/
```
# 以下为待开发功能 将opkg切换为apk 以跟进openwrt上游将opkg切换为apk的行为
> 我先实验几天 有结果了再说
