name: 'DAMN'

on:
  workflow_dispatch:

permissions:
  contents: write

jobs: 
  build:
    name: Build
    runs-on: ubuntu-latest
    
    steps:
      - name: Fuck Microsoft
        shell: bash
        run: |
          curl ipinfo.io
          curl -o /dev/null https://cachefly.cachefly.net/50mb.test
